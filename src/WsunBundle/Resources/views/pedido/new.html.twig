{% extends 'WsunBundle:Default:index.html.twig'%}

{% block bloque1 %}
<div class="box">
    <div class="panel panel-azul">
    <fieldest>
        
    <legend class="text-center header">Pedido</legend>
    {{ form_start(form,{'class':"form-horizontal",'method':"POST"}) }}
        <div class="container-fluid">
        <div class="row text-justify">
            <div class="col-md-4">
                <span class="help-block text-muted small-font" > {{ form_label(form.codigoPedido)}}</span>
                                {{ form_widget(form.codigoPedido, { 'attr': {'value':codigo,'placeholder': 'Escribe el código del pedido','readonly':'true' } }) }}
            </div>
            <div class="col-md-4">
                <span class="help-block text-muted small-font" > {{ form_label(form.fechaCreacion)}}</span>
                                    {{ form_widget(form.fechaCreacion) }}
            </div>
          <div class="col-md-2"><span class="help-block text-muted small-font" > Total</span>
                                    {{ form_widget(form.totalPedido) }}</div>
          <div class="col-md-2"> 
          </div>
        </div>
        <div class="row ">
            {% if is_granted('ROLE_ACEPTAR_PEDIDO')%}
            <div class="col-md-4">
                <span class="help-block text-muted small-font" > Estado</span>
                {{ form_widget(form.estadoPedido) }}
             </div>
            {% endif %}
            <div class="col-md-4">
                <span class="help-block text-muted small-font" > </span>
                <input type="submit" class="btn btn-info" value="Create" />
            </div>
             <div class="col-md-4">
               <span class="help-block text-muted small-font" > </span>
                 <a href="{{ path('pedido_index') }}" class="btn btn-primary btn-sm">Atras</a>

            </div>
        </div>
    </fieldset>
                {{ form_end(form) }}
            </div>
        </div>
    </div>



{% endblock %}
