{% extends 'WsunBundle:Default:index.html.twig'%}

{% block bloque1 %}
    <legend class="text-center header">LISTA DE PEDIDO</legend>
<table class="table table-hover table-inverse">
  <thead class="thead-inverse">
    <tr>
      
    <th scope="row">Código</th>
    <th scope="row">Estado</th>
    <th scope="row">Total Pedido</th>
    <th scope="row">Acciones</th>
    </tr>
  </thead>
  <tbody>
    {% for pedido in pedidos %}
            <tr>
{#                <td><a href="{{ path('pedido_show', { 'id': pedido.id }) }}">{{ pedido.id }}</a></td>#}
                <td>{{ pedido.codigoPedido }}</td>
                <td>{% if pedido.estadoPedido %}Aprobado{% else %}Pendiente{% endif %}</td>
                <td>{{ pedido.totalPedido }}</td>
                <td>
                    <div class="row">
                        <div class="col-md-3">
                         <a href= "{{ path('pedido_show', { 'id': pedido.id }) }}" class="btn btn-primary btn-sm">show</a>
                        </div>
                         <div class="col-md-3">
                         <a href= "{{ path('pedido_edit', { 'id': pedido.id }) }}" class="btn btn-primary btn-sm">edit</a>
                        </div>
                        <div class="col-md-3">
                         <a href= "{{ path('pedido_registro',{ 'id':pedido.id }) }}" class="btn btn-primary btn-sm">products</a>
                        </div>
                    </div>
                    </td>
            </tr>
        {% endfor %}
  </tbody>
</table>
{% endblock %}
