{% extends 'WsunBundle:Default:index.html.twig'%}

{% block bloque1 %}
<div class="box">
    
        <fieldset>
             <div class="panel-heading">
                <strong><span>Lista</span> de Empresas</strong>
            </div> 
            <table class="table table-condensed table-striped">
    <thead>
    <tr>
            <th>Id</th>
            <th>Nombre del Producto</th>
            <th>Capacidad</th>
            <th>Estado</th>
            
            <th>Actions</th>
        </tr>   
  </thead>
  <tbody>
        {% for empresa in empresaP %}
            <tr>
                <td><a href="{{ path('empresa_show', { 'id': empresa.id }) }}">{{ empresa.id }}</a></td>
                <td>{{ empresa.producto.nombreProducto }}</td>
                <td>{{ empresa.capacidad }}</td>
                <td>
                    {% if (empresa.producto.estado)=='1' %}
                        Activo
                    {%else%} 
                        Inactivo
                    {% endif %}
                    
                </td>
                   <td>
                    <div class="row">
                        <div class="col-md-3">
                         <a href= "{{ path('admin_empresa_producto_delete', { 'id': empresa.id }) }}" class="btn btn-primary btn-sm">Borrar</a>
                        </div>
                         
                    </div>
                    </td> 
                                        
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            <a href="{{ path('empresa_new') }}"> <span class="glyphicon glyphicon-file"> Nuevo</span></a>
    </div>        
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>    
    $('#btn1').on("click",function(){
        
    });
    </script>
    
{% endblock %}