{% extends 'WsunBundle:Default:index.html.twig'%}

{% block bloque1 %}
<div class="box">
    <div class="panel-body">
    <div class="panel2">
            <div class="panel-heading">
                <h3> <strong><span>Nueva</span> Categoria</strong></h3>
            </div>
    {{ form_start(form) }}
        <div class="row">
        <div class="col-xs-4 col-sm-3 col-lg-5 text-left">{{ form_label(form.nombreCat)}} </div>
        <div class="col-xs-5 col-sm-6 col-lg-4 text-left">{{ form_widget(form.nombreCat) }}</div>
    </div>        
    <div class="row">
        <div class="col-xs-4 col-sm-3 col-lg-5 text-left">{{ form_label(form.estado)}}  </div>
        <div class="col-xs-5 col-sm-6 col-lg-4 text-left">{{form_widget(form.estado)}}</div>
    </div>
    <div class="row">
        <div class="col-xs-4 col-sm-3 col-lg-5 text-left">Principal  </div>
        <div class="col-xs-5 col-sm-6 col-lg-4 text-left">{{form_widget(form.padre)}}</div>
    </div>
       <div class="row">
          <input type="submit" value="Create" />
      </div>
    {{ form_end(form) }}
    </div> 
    <a href="{{ path('admin_categoria_index') }}">Back to the list</a>
 </div> </div>   
    
{% endblock %}
