{% extends 'WsunBundle:Default:index.html.twig'%}

{% block bloque1 %}
    <div class="box">
           <div class="container">
               <div class="well well-sm">
               
                {{ form_start(form,{'class':"form-horizontal",'method':"POST"}) }}
                    <fieldset>
                        <legend class="text-center header">DETALLE PEDIDO</legend>
                        <div class="row ">
                           
                               <div class="col-md-4">
                                <span class="help-block text-muted small-font" > </span>
                                    {#{ form_widget(form.codigoPedido, { 'attr': {'placeholder': 'Escribe el código del pedido' } }) }#}
                               </div>
                           <h1>Detallepedido creation</h1>

        {{ form_widget(form) }}
        <input type="submit" value="Create" />
  
                        
                        </div>
                    </fieldset>
                {{ form_end(form) }}
            </div>
           </div>
    </div>
    
    

    <ul>
        <li>
            <a href="{{ path('detallepedido_index',{ 'id':id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
